name: Pipeline Testing and Building

on:
  push:
    branches:
      - pbuild

jobs:
  pbuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print GIT SHA Test
        run: |
          echo "${{ github.sha }}"
